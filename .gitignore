# Local-only, may contain sensitive business/contact info
config.local.md

# Secrets — never commit real .env files
.env
2-server/.env

# Transcription working files (large, regenerable, may contain raw audio)
1-voice-profile/downloads/
1-voice-profile/*.mp3
1-voice-profile/*.mp4
1-voice-profile/*.wav
1-voice-profile/whisper-models/

# Third-party agent skills installed via `npx skills add` — tooling, not project code
.agents/
.claude/skills/
skills-lock.json

# npm packages — restorable from package-lock.json with `npm ci`
node_modules/

# OS/editor cruft
.DS_Store
*.swp

# ruflo/claude-flow runtime state — counters, not project content
.claude-flow/

# marketing site — dropped, dashboard is the only site wanted
5-website/
